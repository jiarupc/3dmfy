<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dmfy.Dao.UserDao">
    <!-- 为Dao提供sql配置 -->
    <select id="selectIdByUsername" resultType="Integer">
        select ID
          from user
        where username = #{username}
    </select>

    <select id="selectUser" resultType="User" >
        select ID, username, password, realname
          from user
        where username = #{username}
          and password = #{password}
    </select>

    <insert id="insertUser">
        insert into user(username , password, realname, dept, sex)
        values(#{username}, #{password}, #{realname}, #{dept}, #{sex})
    </insert>
</mapper>